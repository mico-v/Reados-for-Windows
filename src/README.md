# Source Layout

The WinUI app source will live here after the environment is installed.

Expected first project:

```text
src/ReadOS.App
```

Create it with Visual Studio using the `WinUI Blank App (Packaged)` C# template, or with Template Studio for WinUI if we decide to generate an MVVM shell.

