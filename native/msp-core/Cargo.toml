[package]
name = "msp-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Portable core runtime boundary for Model Shell Proxy."

[lib]
name = "msp_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
