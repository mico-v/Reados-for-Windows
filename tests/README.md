# Tests

Test projects will live here once the WinUI solution is scaffolded.

Initial target:

```text
tests/ReadOS.App.Tests
```

The first tests should cover ViewModel state transitions and service-level logic before UI automation is added.

