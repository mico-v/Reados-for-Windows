# ExampleMinimal Renderer

This renderer is a small contribution example. It consumes MSP canonical
timelines and runtime events directly. It does not know about the Default
renderer's Readex-derived payload.

Use it as a reference for third-party renderer structure, not as the polished
Default user experience.
